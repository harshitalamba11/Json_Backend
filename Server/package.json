{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "start": "json-server --watch ./db.json --port $PORT --host 0.0.0.0",
  "postinstall": "npm install -g json-server"
},

  "dependencies": {
    "json-server": "^0.17.3"
  }
}